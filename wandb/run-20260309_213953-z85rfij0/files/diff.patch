diff --git a/Part1.py b/Part1.py
index 5ff617f..3576997 100644
--- a/Part1.py
+++ b/Part1.py
@@ -10,6 +10,7 @@ import torch.optim as optim
 from torch.utils.data.sampler import WeightedRandomSampler
 torch.manual_seed(0)
 
+
 from captum.attr import IntegratedGradients
 from captum.attr import DeepLift
 from captum.attr import NoiseTunnel
@@ -18,6 +19,7 @@ from captum.attr import visualization as viz
 import torchvision
 from torchvision import datasets, transforms
 from torch.utils.tensorboard import SummaryWriter
+from torchvision.models import resnet18, ResNet18_Weights
 writer = SummaryWriter()
 
 import os
@@ -31,6 +33,8 @@ import pandas as pd
 from sklearn.metrics import confusion_matrix
 import seaborn as sns
 
+from torchvision.models import resnet18, ResNet18_Weights
+
 import wandb #comment this out if you are not using weights and biases
 import random #comment this out if you are not using weights and biases
 
@@ -158,7 +162,7 @@ images = "/projects/bgmp/shared/Bi625/ML_Assignment/Datasets/Whale_species/speci
 
 
 transform = transforms.Compose([
-            transforms.Resize([224,224]), # Resize the image as our model is optimized for 224x224 pixels
+            transforms.Resize([320,320]), # Resize the image as our model is optimized for 224x224 pixels
             transforms.ToTensor(),
             transforms.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225])]) # Normalize the data, these are the values that ResNet suggests based on their training data (natural scences))
 
@@ -180,8 +184,8 @@ train_weights = _get_weights(train_set,all_images)
 train_sampler = WeightedRandomSampler(train_weights, len(train_weights))
 
 batchsize = 32
-learning_rate=1e-4
-epochs=10
+learning_rate= 1e-4
+epochs= 10
 
 train_loader = DataLoader(train_set, batch_size=batchsize, drop_last=True, sampler=train_sampler)
 val_loader = DataLoader(val_set, batch_size=batchsize, drop_last=True, shuffle=True)
@@ -191,12 +195,18 @@ test_loader = DataLoader(test_set, batch_size=batchsize, drop_last=True, shuffle
 train_weights = _get_weights(train_set,all_images)
 train_sampler = WeightedRandomSampler(train_weights, len(train_weights))
 
-train_loader = DataLoader(train_set, batch_size=48, drop_last=True, sampler=train_sampler)
-val_loader = DataLoader(val_set, batch_size=48, drop_last=True, shuffle=True)
-test_loader = DataLoader(test_set, batch_size=48, drop_last=True, shuffle=True)
+train_loader = DataLoader(train_set, batch_size=batchsize, drop_last=True, sampler=train_sampler)
+val_loader = DataLoader(val_set, batch_size=batchsize, drop_last=True, shuffle=True)
+test_loader = DataLoader(test_set, batch_size=batchsize, drop_last=True, shuffle=True)
 
 ## Getting our model and transferring it to the GPU
-model = ResNet18().to(device)
+weights = ResNet18_Weights.IMAGENET1K_V1
+model = resnet18(weights=weights) 
+
+num_classes = len(all_images.classes)
+model.fc = nn.Linear(model.fc.in_features, num_classes)
+
+model = model.to(device)
 
 criterion = nn.CrossEntropyLoss()
 optimizer = optim.Adam(model.parameters(), lr=learning_rate)
@@ -205,10 +215,10 @@ optimizer = optim.Adam(model.parameters(), lr=learning_rate)
 wandb.init(
     project="BGMP_HappyWhale",
     name="Alex2-HappyWhale-SpeciesDataset-hyperparameters", ### Update with your name!
-    config={"learning rate":0.001,
+    config={"learning rate":learning_rate,
         "architecture": "CNN",
         "dataset": "Species",
-        "epochs":5}
+        "epochs":epochs}
 )
 
 num_epochs = epochs
diff --git a/wandb/debug-internal.log b/wandb/debug-internal.log
index 8b85b94..4d33a4b 120000
--- a/wandb/debug-internal.log
+++ b/wandb/debug-internal.log
@@ -1 +1 @@
-run-20260301_235007-4gx5j1oq/logs/debug-internal.log
\ No newline at end of file
+run-20260309_213953-z85rfij0/logs/debug-internal.log
\ No newline at end of file
diff --git a/wandb/debug.log b/wandb/debug.log
index 093a393..18470d1 120000
--- a/wandb/debug.log
+++ b/wandb/debug.log
@@ -1 +1 @@
-run-20260301_235007-4gx5j1oq/logs/debug.log
\ No newline at end of file
+run-20260309_213953-z85rfij0/logs/debug.log
\ No newline at end of file
diff --git a/wandb/latest-run b/wandb/latest-run
index 7c2bba5..d6704b3 120000
--- a/wandb/latest-run
+++ b/wandb/latest-run
@@ -1 +1 @@
-run-20260301_235007-4gx5j1oq
\ No newline at end of file
+run-20260309_213953-z85rfij0
\ No newline at end of file
